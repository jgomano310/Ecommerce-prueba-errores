@page
@model Ecommerce.Pages.EstadoPago.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoPago[0].MdUuid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoPago[0].MdDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoPago[0].DescEstadoPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoPago[0].Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CatEstadoPago) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MdUuid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MdDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescEstadoPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CodEstadoPago">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CodEstadoPago">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CodEstadoPago">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
