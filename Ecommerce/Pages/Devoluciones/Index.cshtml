@page
@model Ecommerce.Pages.Devoluciones.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CatDevolucionesPedido[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatDevolucionesPedido[0].MdUuid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatDevolucionesPedido[0].MdDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatDevolucionesPedido[0].DescDevolucion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CatDevolucionesPedido) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MdUuid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MdDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescDevolucion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CodDevolucion">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CodDevolucion">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CodDevolucion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
