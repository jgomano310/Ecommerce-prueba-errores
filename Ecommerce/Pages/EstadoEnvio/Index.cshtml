@page
@model Ecommerce.Pages.EstadoEnvio.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoEnvio[0].MdUuid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoEnvio[0].MdDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoEnvio[0].DescEnvio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CatEstadoEnvio[0].Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CatEstadoEnvio) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MdUuid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MdDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescEnvio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CodEstadoEnvio">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CodEstadoEnvio">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CodEstadoEnvio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
